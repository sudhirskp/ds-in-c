#include<iostream>
using namespace std;
int main(){
    string first,last,name;

    //if name not taken then

    //cout<<"Enter your first and last name: ";
    //cin>>first>>last;
    //first.append(" "+last);

    //if only one varibale availble then suppose name only

    cout<<"Enter your name: ";
    getline(cin,name);


    //name = first + " " + last;
    cout<<"hello ,"<<name<<"!";
    return 0;
}